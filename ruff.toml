line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501"]  # line length handled by formatter

[lint.isort]
known-first-party = ["mlops_drift"]
