# DELIVERABLE, NOT DEPLOYED here (no cluster — spec §0.3).
apiVersion: v1
kind: Service
metadata:
  name: intrusion-serving
  labels: { app: intrusion-serving }
spec:
  selector: { app: intrusion-serving }
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
