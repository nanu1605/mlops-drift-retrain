apiVersion: v1
kind: Service
metadata:
  name: serving
  namespace: mlops
  labels: { app: serving }
spec:
  selector: { app: serving }
  ports:
    - name: http        # named port the ServiceMonitor scrapes
      port: 8000
      targetPort: 8000
  type: ClusterIP
